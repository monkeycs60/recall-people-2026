interface:
  display_name: "Seed Emulator Contacts"
  short_description: "Seed Recall emulator demo contacts"
  default_prompt: "Use $seed-emulator-contacts to populate the current Android emulator with rich Recall demo contacts and events."
