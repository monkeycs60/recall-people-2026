interface:
  display_name: "Seed Emulator Contacts"
  short_description: "Seed Recall through the app UI"
  default_prompt: "Use $seed-emulator-contacts to create six realistic Recall contacts through the Android emulator UI/API flow."
